# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
data
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code's local working directory (worktrees, settings) -- never repo content.
# Left untracked, this shows up as a permanently "dirty" working tree, which is what
# blocked deploy.sh's uncommitted-changes safety check for several deploy attempts.
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
